# Contributing

感谢关注 Gold Price Monitor。这个项目面向黄金积存金行情监控、外部时序模型验证和可解释买卖点观察。欢迎提交数据源适配、模型验证、前端体验、测试和文档改进。

## 开发原则

- 风控优先：任何买点提示都必须经过数据质量、回测、事件风险和仓位约束。
- 可解释优先：新增模型或策略必须能说明输入、输出、阈值、失败场景和回测表现。
- 防未来函数：历史验证只能使用当时已知数据，不能把未来价格、未来事件结果泄露给模型。
- 样本隔离：`historical`、`live`、`production` 样本必须分开记录，不允许历史拟合结果直接放大实盘信号。
- 隐私安全：不要提交真实 token、API key、账号信息、本地绝对路径、个人交易记录或未经授权的数据。

## 本地准备

```bash
npm install
npm run test --workspace server
npm run lint --workspace web
npm run build
```

如需本机运行 Chronos-Bolt 推理服务：

```bash
npm run dev:ai
```

## 提交建议

- `feat:` 新功能。
- `fix:` 缺陷修复。
- `docs:` 文档更新。
- `test:` 测试新增或修复。
- `refactor:` 不改变行为的结构调整。
- `chore:` 构建、依赖、脚本或仓库维护。

## Pull Request 清单

- 说明改动目的和用户影响。
- 列出验证命令和结果。
- 如果改动策略或模型，说明样本口径、评估指标和风险。
- 如果新增数据源，说明来源、频率限制、字段映射和 fallback 行为。
- 确认没有提交 `.env`、本地数据库、报告、缓存、venv、token 或个人路径。

## 投资提示边界

本项目只提供行情观察、模型验证、风险提示和研究工具，不构成投资建议、收益承诺或买卖指令。任何贡献都不得把模型输出描述成确定性收益或保证胜率。
