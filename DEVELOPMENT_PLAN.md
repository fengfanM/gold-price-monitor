# Gold Price Monitor 专业化开发计划

## 目标定位

把当前“可用的金价监控网页”升级为接近同花顺级体验的专业贵金属看盘终端：

- 数据层：工银积存金优先，多源校准，异常报价识别，质量评分透明。
- 存储层：稳定保留长期分钟级行情，支持冷启动恢复和 K 线重建。
- 图表层：专业 K 线、分时、指标叠加、交互读数和移动端适配。
- 策略层：从规则评分升级为可解释、可回测、可迭代的买点策略引擎。
- 提醒层：对急跌和强买点做分级强提醒，后续支持订阅和推送。
- 运营层：GitHub、Vercel/Node 部署、验收、监控和回滚流程明确。

## 总体里程碑

### M1 数据可靠性底座

- 建立统一行情源模型：`official`、`fallback`、`sge`、`external`、`fx`。
- 增加数据质量评分：新鲜度、源健康、跨源偏差、跳点风险、样本完整度。
- 增加异常报价过滤：极端跳点、无效区间、时间倒流、重复点、跨源偏离。
- 扩展 API 输出：返回 `quality`、`sources`、`anomalies`、`calibration`。
- 验收标准：任一源失败时页面不空白，质量降级可见；异常数据不会污染历史。

### M2 持久化历史与后台采集

- 引入持久化存储抽象：先支持本地 JSON，预留 Vercel KV/Postgres/Upstash 接口。
- 增加分钟级 OHLC 聚合：服务端生成 1m/5m/15m/60m K 线。
- 增加后台采集设计：Node 常驻模式定时采集；Serverless 模式按请求刷新并可接 Cron。
- 增加历史恢复：冷启动从持久存储恢复最近 N 天数据。
- 验收标准：重启服务后历史不丢；K 线不依赖浏览器临时聚合。

### M3 专业图表终端

- 图表增强：MA5/MA10/MA20、MACD、RSI、BOLL、分时均线。
- 交互增强：十字光标读数、缩放、拖拽、最后价格线、指标面板切换。
- 信息架构增强：行情头、盘口概览、源质量、策略信号、风险提示分层展示。
- 移动端增强：窄屏下图表优先、侧栏折叠、强提醒不遮挡关键价格。
- 验收标准：K 线不只是展示价格，而是可读、可交互、可辅助判断。

### M4 买点策略引擎

- 抽象策略输入：K 线指标、24h 位置、短线趋势、跨源折溢价、数据质量。
- 输出策略解释：分数、等级、触发条件、反证风险、建议复核项。
- 增加回测框架：对历史数据重放策略，输出命中后走势、最大回撤、失败样本。
- 增加强提醒：`watch`、`strong`、`critical` 分级顶部横幅/弹层/声音开关。
- 验收标准：买点信号必须可解释、可降级、可回放，不承诺收益。

### M5 新闻情绪与观点输入

- 新闻输入：接入公开财经新闻/RSS/搜索摘要，做来源、时间、主题分类。
- 情绪评分：利多/利空/中性、宏观变量标签、可信度评分。
- 博主观点：建立观点采集接口和可信度维度，不把单一观点作为强信号。
- 策略融合：新闻情绪只作为加权输入，不覆盖价格和数据质量约束。
- 验收标准：页面能展示“为什么情绪加分/扣分”，且来源可追溯。

### M6 部署验收与监控

- CI 验收：build、lint、核心单测、策略回放样例。
- 部署验收：Vercel 预览、公网 smoke test、API 响应时间和数据完整性检查。
- 运行监控：健康接口返回上游错误、源状态、最近刷新耗时、历史点数量。
- 回滚策略：部署失败或行情源异常时可回退到上一个稳定版本。
- 验收标准：公网可访问、API 稳定、关键异常有可见告警。

## Worktree/Subagent 分派

当前 IDE 没有真实 subagent 调度器；执行上采用“隔离 worktree + 任务泳道 + 验收后合并回收”的方式模拟并行开发。

| Subagent | Worktree | 职责 | 依赖 | 回收条件 |
| --- | --- | --- | --- | --- |
| data-agent | `../gold-monitor-data` | 多源模型、数据质量、异常过滤、API 扩展 | 当前服务层 | 单测+build 通过，API 向后兼容 |
| storage-agent | `../gold-monitor-storage` | 持久化抽象、历史恢复、服务端 K 线聚合 | data-agent 的样本模型 | 重启后历史恢复，K 线稳定 |
| chart-agent | `../gold-monitor-chart` | 专业指标、图表交互、终端 UI | storage-agent 的 K 线 API | 前端 build/lint 通过，移动端可用 |
| strategy-agent | `../gold-monitor-strategy` | 买点策略引擎、解释、回测样例 | data-agent + storage-agent | 策略输出可解释，可回放 |
| alert-agent | `../gold-monitor-alerts` | 急跌/买点强提醒、声音/订阅预留 | strategy-agent 输出 | 分级提醒清晰，不阻塞看盘 |
| ops-agent | `../gold-monitor-ops` | CI、部署验收、健康监控、文档 | 所有合并结果 | 公网 smoke test 通过 |

## 合并顺序

1. `data-agent` 先合并：没有可靠数据，后续图表和策略都会误导。
2. `storage-agent` 第二：解决公网历史断档，为专业 K 线和回测提供基础。
3. `chart-agent` 与 `strategy-agent` 可并行：一个做专业看盘，一个做策略解释。
4. `alert-agent` 基于策略输出增强体验：避免先做强提醒但信号不可靠。
5. `ops-agent` 贯穿全程：每次合并后补充验收和部署检查。

## 近期执行顺序

1. 完成 M1：先在当前代码中加入数据质量评分和异常过滤，不破坏现有 API。
2. 完成 M2：抽象存储层，准备接入 Vercel KV/Postgres 类持久化。
3. 完成 M3 的第一版：MA/BOLL/RSI/MACD 指标和图表面板切换。
4. 完成 M4 的第一版：策略模块化、买点强提醒和本地回放样例。
5. 完成 M6 基线：lint/build/test/smoke test 成为每次发布必跑。

## 风险边界

- 银行公开接口可能延迟、限频、改字段，不能承诺交易级实时。
- 新闻和博主观点只能作为辅助情绪输入，不能替代价格和风险控制。
- 买点信号必须始终标明“观察用途，不构成投资建议”。
- 多源数据若互相冲突，优先展示质量降级和风险，不强行给确定结论。
